@model DotNetQuiz.Web.Models.LoginViewModel

@{
    ViewBag.Title = "Login";
}

@using(Html.BeginForm())
{
    @Html.ValidationSummary(true)
    @Html.EditorForModel()
    @Html.AntiForgeryToken()
    
    <input type="submit" value="Log in" />
}